# Changelog

本文件记录用户可见的功能与修复。

## [0.4.8] - 2026-09-13

- 大表首次搜索和筛选按时间预算分批计算，显示进度；扫描期间暂停冲突操作，完成后统一更新结果，避免长时间占用界面线程。
- 一万行以内沿用直接计算，减少小文件调度开销；相同查询的筛选复用完整查找结果，编辑后自动使缓存失效。
- 保留反向查找、原有行顺序及空节标题处理，退出时取消尚未完成的扫描；保存后的内部刷新保持同步，确保“全部保存”仍写回两侧。

## [0.4.7] - 2026-09-13

- GUI 文件读取、解析与对齐移到后台，主表每批显示 300 行；读取和显示期间暂停编辑、保存及重复加载，读取失败保留旧内容，退出后忽略后台结果。
- 编辑后筛选仅更新该行与所属节的可见状态，避免整表重复筛选；每次查询只归一化搜索词一次。
- GUI 与命令行共用读取、对应区段选择及编码回退逻辑，支持 MSG0/MSG1 等对应文本表；保留各入口显式编码的既定策略。
- 合并 AINI 和混合编码写回的数字 ID 补丁解析，清理无用状态和计数；构建统一使用 `aindiff.spec`，不再重新生成或覆盖该配置。

## [0.4.6] - 2026-09-13

- 修复ランス０２等混合编码 AIN 写回时的 `iconv` 错误：常规写回遇到编码转换失败时，自动将修改文本严格编码后通过字节保真方式交给 alice 写回，保留函数名等其他内容的原始字节。
- ランス０２的 MSG0、STR0 已通过副本编辑、全表比对、备份与恢复验证；函数级读取仍使用原始表回退，尚未验证游戏内运行效果。
- 重试失败或文本无法用目标编码表示时保留文件、已有备份及未保存编辑；不对其他类型的 alice 错误重试。

## [0.4.5] - 2026-09-13

- 修复编辑后最后一个匹配行被隐藏时仍留下空节标题的问题，清除筛选后保持原有顺序。
- 原始文本表和旧 AINI/AIN2 读取统一严格解码，编码不匹配时报告失败，避免把替换字符带入可编辑文本；视图切换失败保留已有编辑。
- 更新按钮、快捷键和维护说明，明确混合编码文件可读取不代表可保存，ランス０２目前不支持写回。
- 发布构建自动生成 SHA-256 校验文件，并修正缺少 alice 时的准备说明。

## [0.4.4] - 2026-09-12

- 实际 GUI 验证后调整默认双栏宽度、中文字体和行距，多行编辑框自动换行并按字体高度适配；查找框增加 Shift+Enter 反向查找。
- 窗口缩放和切换结构栏时自动均分文本列；查找栏自适应宽度，底部操作提示和状态信息在窄窗口自动换行。

- 保存按钮与左右文件对应排列，主界面直接提供差异筛选、备份开关和全部保存入口。
- 文本表下方增加编辑快捷键与行颜色说明；结构树使用侧栏完整宽度。
- 修复结构栏隐藏后无法正确恢复，以及恢复到右侧的问题。
- 修复搜索框内 F3 重复定位，补齐 Shift+F3 和 F5 快捷键。
- 筛选时隐藏没有匹配行的节标题，清除筛选后恢复原有顺序。

## [0.4.3] - 2026-09-12

- 更新 README、详细使用说明和发布包简明说明，明确编码回退、备份覆盖和筛选行为；发布包补齐详细文档及更新记录。
- GUI 原始区段回退保留手动选择的编码；编辑后立即隐藏不再满足搜索或差异筛选条件的行。
- 修复 AINI 写回丢失未知尾部、改写保留字段，以及注释赋值被误执行的问题；增加截断数据、非法补丁和 NUL 校验。
- 保存使用独立临时文件；拒绝空的 alice 输出。TSV 导出避免覆盖输入 AIN，并在失败时保留已有结果。
- 修复特殊控制字符和 Unicode 分隔符导致文本漏读的问题；编码回退使用严格解码并复用区段表和解密数据。
- CLI 回退对照统一使用对应原始表，切换表时重新探测编码，常见文件和编码错误返回清晰提示。
- 修复 GUI 隐藏行清理、搜索定位、未提交单元格保存、重复 ID 同步、脏标记及读取失败状态保留。
- 修复切换视图时已确认丢弃的编辑在缓存中再次出现的问题；目标视图读取失败仍保留原编辑。
- 筛选改为批量更新，连续查找缓存命中结果；AINI 位旋转改为批量字节转换。
- 修正 CLI 测试参数、样本边界及 GUI 隔离，补充保存失败、解析、编码和界面状态回归测试。

## [0.4.2] - 2025-09-08

- 修复混合编码 AIN（如ランス０２）读取区段表时误用文本编码的问题。
- 区段表读取增加 CP932/CP936/UTF-8 回退。

## [0.4.1] - 2025-09-08

- 修复单文件模式加载时 `_load_section_maps` 重复定义导致的崩溃。

## [0.4.0] - 2025-09-08

- 内置 xsys35 `AINI` 容器支持：可读取和编辑 `VARI`/`MSGI`。
- 支持 System39.ain 等旧 AINI 文件。

## [0.3.x]

- 单文件模式、AIN 区段视图（MSG0/MSG1/STR0）。
- 查找定位保持原行位置。
- 默认侧栏只显示 AIN 区段。
- Windows 高 DPI 感知。
- 独立解压即用包。

## [0.2.0]

- 查找定位与结构树。

## [0.1.0]

- 首个版本：双 AIN 文本节对照编辑，调用 alice-tools 写回。
