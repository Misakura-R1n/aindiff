# Changelog

本文件记录用户可见的功能与修复。

## [0.4.3] - 2026-09-12

- 更新 README、详细使用说明和发布包简明说明，明确编码回退、备份覆盖和筛选行为；发布包补齐详细文档及更新记录。
- GUI 原始区段回退保留手动选择的编码；编辑后立即隐藏不再满足搜索或差异筛选条件的行。
- 修复 AINI 写回丢失未知尾部、改写保留字段，以及注释赋值被误执行的问题；增加截断数据、非法补丁和 NUL 校验。
- 保存使用独立临时文件；拒绝空的 alice 输出。TSV 导出避免覆盖输入 AIN，并在失败时保留已有结果。
- 修复特殊控制字符和 Unicode 分隔符导致文本漏读的问题；编码回退使用严格解码并复用区段表和解密数据。
- CLI 回退对照统一使用对应原始表，切换表时重新探测编码，常见文件和编码错误返回清晰提示。
- 修复 GUI 隐藏行清理、搜索定位、未提交单元格保存、重复 ID 同步、脏标记及读取失败状态保留。
- 修复切换视图时已确认丢弃的编辑在缓存中再次出现的问题；目标视图读取失败仍保留原编辑。
- 筛选改为批量更新，连续查找缓存命中结果；AINI 位旋转改为批量字节转换。
- 修正 CLI 测试参数、样本边界及 GUI 隔离，补充保存失败、解析、编码和界面状态回归测试。

## [0.4.2] - 2025-09-08

- 修复混合编码 AIN（如ランス０２）读取区段表时误用文本编码的问题。
- 区段表读取增加 CP932/CP936/UTF-8 回退。

## [0.4.1] - 2025-09-08

- 修复单文件模式加载时 `_load_section_maps` 重复定义导致的崩溃。

## [0.4.0] - 2025-09-08

- 内置 xsys35 `AINI` 容器支持：可读取和编辑 `VARI`/`MSGI`。
- 支持 System39.ain 等旧 AINI 文件。

## [0.3.x]

- 单文件模式、AIN 区段视图（MSG0/MSG1/STR0）。
- 查找定位保持原行位置。
- 默认侧栏只显示 AIN 区段。
- Windows 高 DPI 感知。
- 独立解压即用包。

## [0.2.0]

- 查找定位与结构树。

## [0.1.0]

- 首个版本：双 AIN 文本节对照编辑，调用 alice-tools 写回。
